# Шаблон сообщений для правки записей в Sheets

Менеджер может в любой момент исправить или дописать данные в таблицах
«Рейсы», «Закупки», «Склад» — просто отправить сообщение в группу
«Производство Тихорецкая». Бот понимает свободную форму, но желательно
использовать триггерные слова в начале.

## Триггерные слова

В сообщении ОБЯЗАТЕЛЬНО должно быть одно из слов:
- **Поправь** / **Поправь:**
- **Исправь** / **Исправь:**
- **Правка** / **Правка:**
- **Корректировка**
- **Уточнение**
- **Дополни**
- **Обнови**

## Шаблон правки закупки

```
Поправь закупку #2 — поставщик Великовечное, цена закупки 40, 20 поддонов 1500 шт, доставка от поставщика 18000
```

или с датой и клиентом (если номер забыли):

```
Корректировка закупки 14.05 ИП Горячкина — добавь 1000 шт, 15 поддонов, цена 41, доставка 15000
```

**Что можно править в закупке:**
- `поставщик` — кто продал нам
- `откуда` — откуда привезли (город/база)
- `продукция` — какой блок
- `шт` — количество штук
- `подд` — количество поддонов
- `цена_закупки` — за сколько купили (руб/шт)
- `доставка_от_поставщика` — стоимость доставки до нас
- `клиент` — кому потом перепродали
- `дата_продажи` — когда продали
- `цена_продажи` — за сколько продали (руб/шт)
- `доставка_клиенту` — стоимость доставки клиенту

## Шаблон правки рейса

```
Исправь рейс #5 — водитель не Кораблев, а Адлейба
```

```
Поправь рейс #8 — куда не Шубина, а Деревянко, организация ИП Деревянко
```

**Что можно править в рейсе:**
- `дата`, `машина`, `водитель`, `откуда`, `куда`, `организация`, `продукция`, `источник`

## Как бот ищет нужную строку

В порядке приоритета:
1. **По номеру записи** (если назван `#2`, `№5`)
2. **По дате + клиенту** (если номер не указан)
3. **Только по дате** (если клиента не назвал)

Если несколько записей за один день и клиент не указан — берётся **первая** найденная. Лучше всегда указывать номер.

## Ответ бота

После применения правки бот отвечает:
- `✏️ Правка Закупки (строка 5): цена_закупки=40, подд=20, шт=1500`
- `⚠️ Не нашёл запись в Закупки (num=20, date=...)` — если не найдено
- `⚠️ Неизвестные поля: ...` — если назвал поле которого нет

## Примеры реальных правок (для текущих незакрытых строк)

**Дополни закупку #1 от 12.05** (нужно от менеджера: поставщик, кол-во, цены):
```
Поправь закупку #1 от 12.05 — поставщик [имя], цена закупки [руб/шт], [N] поддонов [M] шт, доставка [руб]
```

**Дополни закупку #2 от 13.05**:
```
Поправь закупку #2 от 13.05 — поставщик Великовечное, цена закупки [руб/шт], [N] поддонов, доставка [руб]
```

**Дополни закупку #3 от 14.05**:
```
Поправь закупку #3 от 14.05 — поставщик Великовечное, цена закупки [руб/шт], [N] поддонов, доставка [руб]
```

Когда есть **продажа** — добавляем цену продажи и доставку клиенту:
```
Дополни закупку #3 — дата продажи 14.05, цена продажи 55, доставка клиенту 12000
```

После этого формулы листа автоматом пересчитают сумму закупки, сумму продажи, итого расходы и маржу.
